[build]
  publish = "public"
  functions = "netlify/functions"

[dev]
  publish = "public"
  port = 8888

# Redirige todo a index.html (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
